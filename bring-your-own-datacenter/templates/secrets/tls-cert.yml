{{- if .Values.ambassador.config.tls.enabled -}}
apiVersion: v1
kind: Secret
type: kubernetes.io/tls
metadata:
  name: tls-cert
  namespace: {{ .Release.Namespace }}
data:
  {{- if .Values.ambassador.config.tls.crtBase64 }}
  tls.crt: {{ .Values.ambassador.config.tls.crtBase64 }}
  {{- else }}
  tls.crt: {{ .Values.ambassador.config.tls.crt | b64enc }}
  {{- end }}

  {{- if .Values.ambassador.config.tls.keyBase64 }}
  tls.key: {{ .Values.ambassador.config.tls.keyBase64 }}
  {{- else }}
  tls.key: {{ .Values.ambassador.config.tls.key | b64enc }}
  {{- end }}
{{- end -}}
